<div class="media my-3 ml-2">
  <%= link_to image_tag(user.gravatar_url, class: "rounded-circle mr-2", style: "width: 2rem;" ), users_show_path(user) %>
  <div class="media-body">

    <small class="mt-0"><b> <%= link_to user.name, users_show_path(user), class: "text-muted" %></b></small> 
  </div>
</div>